When you create an ACF field group in the wp-admin, by default
it will save the field config as a file in this folder. 

Please commit these files to the repository so that the ACF config can be easily synced
across different instances of the site.